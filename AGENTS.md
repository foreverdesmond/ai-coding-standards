# AGENTS.md — Context L1 标准入口

> 规范版本：V2.5
> 文档状态：草稿（待审核）
> 作者：WorkBuddy（受 Hermes 总调度委派）
> 创建日期：2026-08-20
> 最后更新：2026-08-20
> 审核人：Richy（待审）

---

## 1. 定位

本文件是本仓库的 **Context L1 标准入口**（物理载体），为在本仓库上工作的 AI 编码 Agent（WorkBuddy / Codex / 其他）提供一致、可预测的项目级操作说明。

它回答的问题是：进入这个仓库的 Agent 应该知道哪些跨任务稳定的约定、边界和规则。

它与 `文档规范/06-开发上下文包生成与使用规范.md` 衔接：`AGENTS.md` 是 Context L1 的载体，`06` 规定 Context L1/L2/L3 的分层、触发、失效与对账规则。

## 2. 层级与优先级（closest-wins）

`AGENTS.md` 采用分层 + 就近覆盖（closest-wins）机制：

```text
~/.claude/AGENTS.md           # global：跨所有仓库的全局约定
  → ./AGENTS.md               # 项目级：本仓库约定（本文件）
    → ./<subdir>/AGENTS.md    # 子包级：子目录专属约定（按需）
```

冲突消解优先级（从高到低）：

1. **用户当前显式指令** —— 最高，覆盖一切；
2. **就近 `AGENTS.md`** —— 离被编辑文件最近的 `AGENTS.md` 优先；
3. **远程文档 / 上级 `AGENTS.md`** —— 作为兜底补充。

Agent 应读取"离当前工作文件最近的一份 `AGENTS.md`"，而非无差别加载全部层级。

## 3. 项目概览

| 项 | 说明 |
|---|---|
| 仓库性质 | **多 Agent 开发协作规范库**（纯文档仓库，无源代码工程） |
| 技术栈 | 纯 Markdown（`.md`），无构建/编译/测试命令 |
| 规范现状 | V2.5 待审核（V2.3 为上一已审核基线），Hermes 总调度迁移进行中 |
| 调度模式 | **Hermes 常驻总调度** + WorkBuddy / Codex / Human 平行协作执行 |

## 4. 分支策略

| 分支 | 角色 | 说明 |
|---|---|---|
| `main` | 稳定分支 | 已审核基线，由独立合并任务在授权后合入 |
| `V2.5`（远程大写） | 当前开发分支 | Hermes 迁移开发主分支 |

**大小写警示**：本地分支名为小写 `v2.5`，远程分支名为大写 `V2.5`；本地 `v2.5` 未设上游跟踪，推送必须使用显式 refspec：

```text
git push origin v2.5:V2.5
```

## 5. Git 与工具权限（沙箱分级）

依据《Hermes能力边界清单》§6，工具权限按沙箱分级：

| 任务类型 | sandbox | 说明 |
|---|---|---|
| 只读调研 / 代码审核 | `read-only` | Reviewer、只读调查 |
| 写文档 / 写证据 | `workspace-write` | 不触碰 `.git` |
| **需要 git 提交** | `danger-full-access` | `.git` 在 `workspace-write` 下为 protected path，会拦 git |

规则：

- 需要 `git add/commit/merge/push` 的角色（Implementer、Integrator）必须使用 `danger-full-access`；
- **Hermes 不代执行 git**：worktree 创建/commit/merge 由相应角色自办，Hermes 只派发参数、巡检、判 gate；
- 存在冲突的任务不设并行，实施中真冲突需 Richy 协调（《Hermes流程与边界决议》C2）。

## 6. 文档编写约定

本仓库所有规范文档遵循统一约定：

- **文档头**：规范版本、文档状态、作者、创建日期、最后更新、审核人、修订记录（文末表）；
- **文档状态**：`草稿` / `待审核` / `修改中` / `已审核通过` / `待同步` / `已取代` / `已归档`；
- **版本规则**：以 `05-文档审核与变更控制规范.md` §5 为准（`V主.次`）；
- **术语**：代码基线统一使用 `CodeBaseSHA`（旧名 `BaseSHA` 已废弃）；
- **事实与推断分离**：已验证事实、推断、建议、待确认必须分开记录。

## 7. 与本仓库规范的衔接

| 本文件作用 | 对应规范 |
|---|---|
| Context L1 载体 | `文档规范/06-开发上下文包生成与使用规范.md` |
| 上下文包生成责任与时机 | `Hermes流程与边界决议.md` §三（Doc/Design Reviewer，开发前生成、依赖已满足） |
| 调度与台账 | `文档规范/09-Hermes调度与运行时台账规范.md` |
| 角色与提示词 | `文档规范/07-子Agent任务委派与提示词规范.md` + `提示词模板/` |

---

## 修订记录

| 版本 | 日期 | 作者 | 变更说明 |
|---|---|---|---|
| V1.0 | 2026-08-20 | WorkBuddy | 首次创建：确立 Context L1 标准入口、closest-wins 优先级、项目概览、分支策略、沙箱分级、文档约定 |
| V1.1 | 2026-08-20 | WorkBuddy | §3 规范现状更新为 V2.5 待审核（V2.3 为上一已审核基线） |
