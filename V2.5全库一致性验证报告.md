# V2.5 全库一致性验证报告

> 验证范围：ai-coding-standards 仓库 V2.5 分支全量文档（README / Hermes流程与边界决议 / Hermes能力边界清单 / 文档规范 01–09 / 提示词模板 00–15 / AGENTS.md）
> 验证方式：静态全文审读 + 旧词/新术语全局扫描 + 交叉引用有效性检查；未修改任何文件
> 验证人：WorkBuddy（受 Hermes 总调度委派）
> 日期：2026-08-20
> 基线提交：`39d4f87`（P3 批次提交后）

---

## 0. 总览

| 判定 | 数量 | 维度 |
|---|---|---|
| ✅ 通过 | **4** | 台账/状态一致性（3）、恢复一致性（5）、沙箱/权限一致性（6）、测试分层一致性（7） |
| ⚠️ 警告 | **6** | 术语一致性（1）、角色一致性（2）、调度循环一致性（4）、文档头一致性（8）、引用有效性（9）、遗漏/矛盾（10） |
| ❌ 不通过 | **0** | — |

**核心结论**：P0–P3 改造的主体（00 基座、09 台账规范、04/05/07、README、13/14/15 模板、AGENTS）内部高度自洽，V2.5 迁移目标（Hermes 常驻调度 + 台账 + 事件/cron + 单实例幂等 + 三级恢复 + 沙箱分级）在全库已一致落地。**全部警告集中在两类**：① P2/P3 未覆盖的模板文件（`提示词模板/README.md`、`01-开发Agent.md`、`10-开发任务审核Agent.md`）仍是 V2.4 残留；② 少量散点的旧词/旧角色名/状态名未同步。均为局部修补问题，不影响已改造核心，但**建议在 V2.5 定稿前补齐**。

---

## 1. 术语一致性 —— ⚠️ 警告

### 通过项（已统一）
- `ExpectedExecutionKind` 四档枚举 `WorkBuddy / Codex / Human / ApprovedEquivalent`：`00:28`、`04:360`、`07:41`、README `DOC-DEC-024`、09 引用——全库一致，无 `CodexThread/SubAgent` 残留。
- `CodeBaseSHA` 全库统一；`BaseSHA` 仅在「废弃声明」（`05:250`、AGENTS:83）与修订历史中出现，无正文明使用。
- `CoordinatorThreadID` / `clientThreadId` / `CoordinatorConsumedRevision` / `TaskStateWriteLockPath` / `SharedLocalJson` 均无残留（09 中「替代旧 …」「不区分 clientThreadId/threadId」为说明性表述，合理）。
- `ExecutionRef` / `LedgerLocation` / `StateRecordID` / `DispatchKey` / `ParentCoordinatorRef` 在 00/04/07/09/README/模板中分布正确。

### 残留（未同步）
| 位置 | 残留内容 | 影响 |
|---|---|---|
| **`提示词模板/README.md:59`** | `CanonicalTaskDocumentPath、SharedRuntimeStatePath、StateUpdateToolPath、StateRecordID、派发时状态区版本和短时锁` | 该文件是 07 §1 指定的「模板唯一真源」入口，旧词直接影响所有角色注入要求 |
| **`提示词模板/01-开发Agent.md:57`** | 「通过 `StateUpdateToolPath` 把同一结果写入 `SharedRuntimeStatePath` 并发布 `PendingConsumption`」 | 与 `00:64-66`「执行 Agent 不直接写台账」直接矛盾（见维度 10） |
| **`提示词模板/10-开发任务审核Agent.md:21`** | `SharedRuntimeStatePath、StateUpdateToolPath、协调租约、RecoveryOnly（从 JSON 重建）` | 全套 V2.4 控制面检查项 |
| **`文档规范/07:244`** | 质量门禁「已配置唯一状态区、幂等 DispatchKey、协调租约…」 | 协调租约未标注「不设」 |
| **`文档规范/04:321`** | `InvocationID、ExecutionThreadID … 必须写入共享本地状态` | §8.4 尾部残留，与 §8.1–8.3 新语义矛盾 |
| **`README:223`** | §5 阶段地图「文档状态生产/消费、**锁、租约**、恢复和 Canary」 | 与 §2.10/2.11「不再有共享 JSON 文件锁」矛盾 |

---

## 2. 角色一致性 —— ⚠️ 警告

### 通过项
- README §4 收敛为 **6 种核心角色**（Coordinator/Implementer/Reviewer/Integrator/Validator/Doc-Design Reviewer）+ 合并来源说明 + 项目负责人（人类）单独声明 + 保留底线——与 `Hermes流程与边界决议` §B5 完全一致。
- README §6.1 Git 权限矩阵按 6 角色更新，含「Hermes 不代执行任何 git 操作」「Implementer/Integrator 用 danger-full-access、其余只读」。
- `00:31` Role 枚举为 6 种角色，一致。

### 残留（未同步）
| 位置 | 残留 | 说明 |
|---|---|---|
| **`07:23-32`** §2 角色列表 | 仍列 `System Reviewer / Merge Reviewer / Iteration Integrator / Main Merge Executor / Rework Implementer / Requirements-Design-Task Reviewer` 等 10 个扩展角色 | 未注明「已并入 README §4 6 角色」的映射，与 README 收敛口径存在概念张力 |
| **`提示词模板/` 目录** | `03-返工Agent`、`05-系统集成Reviewer`、`06-最终合并审核Agent`、`08/09/10` 审核、`11/12` 合并等 V2.4 角色模板仍为独立文件 | README §4 合并说明已覆盖语义，但模板未收敛/未在文件头注明归属 |
| **`README:260`** §7 验证等级 | Level 1 执行者仍写「**系统集成 Reviewer**」 | 旧角色名，6 角色中已并入 Reviewer（按 Level 区分范围） |
| **`README:159-162`** §2.9 | 「普通开发者和返工执行者」「最终合并审核者」「独立主分支合并执行任务」 | 职责性措辞，非角色表，可接受但建议统一称谓 |

---

## 3. 台账/状态一致性 —— ✅ 通过

- `09 §5` 台账 schema（`LedgerLocation` 本地 JSON + 可选 SQLite、不进 Git；`TASK-STATE-EXCHANGE` 为 Git 持久快照）与 `04 §8.1-8.3`、`00 §4`、`07 §8`、能力清单 §2 完全一致。
- 幂等键 `DispatchKey = IterationID+TaskID+Stage+TargetIdentity` 与消费幂等 `RecordID + SignalRevision` 在 09/04/00/README/07 模板四处统一。
- `SignalState` 四值（None/PendingConsumption/Consumed/Superseded）、`ConsumedRevision`、执行 Agent 不直接写台账——一致。
- 唯一不一致点：README §8.2 用 `Pending`，而 `09 §3.2` 与 `04 §8.4` 均用 `Planned`（README 宣称「统一使用」却与规范不符）。属警告级措辞，并入维度 10 问题清单。

---

## 4. 调度循环一致性 —— ⚠️ 警告

### 通过项
- 「事件驱动（飞书 + Codex 网关轮询）+ cron 兜底对账」双通道：README §2.10/§10.9、09 §6.1/§8/§11、07 模板 §2、13 模板 §1——描述一致。
- 「单实例、无协调租约锁、DispatchKey 幂等去重」：09 §2.7/§12.2、README §10.17、07 模板:25、能力清单 §1——贯穿一致。
- 依赖图由 Hermes 主动触发下游：09 §7/07 模板 §3.9——一致。

### 残留
- **`README:223`** §5 阶段地图仍写「锁、租约」可执行，与 §2.10/§2.11 及 DOC-DEC-022 矛盾（同维度 1 第 6 项）。

---

## 5. 恢复一致性 —— ✅ 通过

- 三级恢复（热自动续跑 / 冷全自动读台账+Git 重建、失败自动升灾难 / 灾难需 Richy 介入批准）：`09 §13`、`14 模板 §1`、能力清单 §7、README §2.11/§10.11、07 模板 §5——五处描述完全一致。
- 最大安全状态规则表：`09 §13.2` 与 `14 模板 §5` 逐条对应。
- 冷恢复步骤：`09 §13.1` 与 `14 模板 §4` 对应一致。

---

## 6. 沙箱/权限一致性 —— ✅ 通过

- 三级沙箱（`read-only` / `workspace-write` / `danger-full-access`）：能力清单 §6、`09 §7.3`、`00 §3`、`AGENTS §5`、README §6.1、`15 模板` Canary 场景 14——六处一致。
- 「需要 git 的角色（Implementer/Integrator）必须 danger-full-access」「`.git` 在 workspace-write 下为 protected path」「Hermes 不代执行 git」——一致。
- `04`/`07` 未含 sandbox 字段（不矛盾，sandbox 属派发参数由 00/09 注入）。

---

## 7. 测试分层一致性 —— ✅ 通过

- 「L0 由 Implementer 跑一次并落证据；Reviewer 基于 diff + L0 证据审查、只抽查关键路径不重跑全套单测；L1 + 回归由 Validator 仅在 merge 前跑全量一次」：`00:101`、`09:281-285`、`Hermes流程与边界决议` §四、README §4 Reviewer 边界——四处一致。

---

## 8. 文档头一致性 —— ⚠️ 警告

### 通过项
- README、规范 01–08、`00`、`07 模板`、`13/14/15 模板`、AGENTS、能力清单、流程决议均带 V2.5 + 状态（待审核/草稿）+ 作者 + 审核人（Richy 待审）+ 文末修订记录。
- 版本号统一 V2.5。

### 不一致
| 位置 | 问题 |
|---|---|
| **`提示词模板/README.md`** | 文档头仍为 **V2.4**、更新日期 2026-08-15、引用旧 09 名称（见维度 9） |
| **提示词模板 `01/02/03/04/05/06/08/09/10/11/12`** | 均无统一文档头（V2.4 遗留；仅 00/07/13/14/15 有） |
| **`AGENTS.md:44`** | 「规范现状：V2.4 待审核…V2.5 为迁移目标版本」未同步 P3 后的状态（README 已升 V2.5 待审核） |

---

## 9. 引用有效性 —— ⚠️ 警告（3 处断链）

| 位置 | 引用 | 问题 |
|---|---|---|
| **`07:12`** | `[09-调度控制平面与运行时台账规范](./09-调度控制平面与运行时台账规范.md)` | 文件已 `git mv` 删除 → **断链**，且 07 §1 声称「以 09 为唯一真源」却指向不存在文件 |
| **`04:27`** | `[09-调度控制平面与运行时台账规范](./09-调度控制平面与运行时台账规范.md)` | 同上断链 |
| **`提示词模板/README.md:6`** | `[09-调度控制平面与运行时台账规范](../09-调度控制平面与运行时台账规范.md)` | 同上断链 |

其余引用有效：README §3 目录表（`./文档规范/09-Hermes调度与运行时台账规范.md`）、`08:66`、`00`、`13:31`、`14`、AGENTS §7 均指向新名且文件存在。

---

## 10. 遗漏/矛盾 —— ⚠️ 警告

按严重程度列出自相矛盾或与 V2.5 目标冲突的残留：

| # | 位置 | 矛盾内容 |
|---|---|---|
| 1 | **`01-开发Agent.md:57`** | 协议头已输出 `StateRecordID/PublishedSignalRevision`（新），57 行却又要求「通过 `StateUpdateToolPath` 写 `SharedRuntimeStatePath` 发布 `PendingConsumption`」——与 `00:64`「执行 Agent **不直接写台账**」直接冲突 |
| 2 | **`10-开发任务审核Agent.md:21-22`** | 审核项要求检查 `SharedRuntimeStatePath/StateUpdateToolPath/协调租约/RecoveryOnly（从 JSON 重建）/Codex 子任务兜底禁令`——整套 V2.4 控制面检查与 09/04 新语义矛盾 |
| 3 | **`提示词模板/README.md:59`** | 「所有异步角色都必须提供 SharedRuntimeStatePath、StateUpdateToolPath、短时锁」与 `00`/`09`（台账接口幂等写入、无锁）矛盾；`:23`「明确要求 Codex 子任务时…不得兜底」为旧 Codex 专属禁令 |
| 4 | `04:321` | `ExecutionThreadID` + 「写入共享本地状态」与同节 §8.1–8.3 矛盾 |
| 5 | `README:223` | 「锁、租约」与 §2.10/2.11 矛盾 |
| 6 | `README:277` | 状态枚举 `Pending` 与 `09/04` 的 `Planned` 不一致（README §8.2 宣称「统一使用」） |
| 7 | `07:245` | 「指定 Codex 子任务时没有未经授权的执行机制兜底」——§2.1 已改「禁止伪独立自审」，此条未同步 |
| 8 | `README:260` | Level 1 执行者「系统集成 Reviewer」旧角色名 |

---

## 11. 问题清单（按严重程度排序 + 修法建议）

### P1（建议 V2.5 定稿前必修）

**Q1. `提示词模板/README.md` 整体仍是 V2.4**（高）
该文件是 07 §1 指定的「各角色模板唯一真源」，旧词会污染每一次派发注入。
修法：文档头升 V2.5；`:6` 调度协议引用改 `09-Hermes调度与运行时台账规范`；`:23` 删 Codex 兜底禁令，改「指定执行机制（WorkBuddy/Codex/Human）不得被未经授权替代，禁止伪独立自审」；`:59` 改「所有异步角色由 Hermes 注入 `LedgerLocation` + `StateRecordID`，通过载体通道输出结构化协议头，由 Hermes 幂等写入台账（事件 + cron 兜底），无文件锁」；`:36/43/44` 的 07/13/14 描述同步为 Hermes 台账/cron 对账/重启恢复表述。

**Q2. `01-开发Agent.md:57` 旧词残留且自相矛盾**（高）
开发 Agent 是最高频模板。
修法：改为「输出 final 前先输出本模板规定的结构化协议头（含 `StateRecordID`），由 Hermes 消费后幂等写入台账；执行 Agent 不直接写台账」。

**Q3. `10-开发任务审核Agent.md:21-22` 全套 V2.4 控制面检查项**（高）
修法：改为「检查 Hermes 台账（`LedgerLocation`/`CanonicalTaskDocumentPath`/Schema）可用、幂等 `DispatchKey`、事件 + cron 对账、暂停、三级恢复与适用 Canary 可执行；指定执行机制不得被未经授权替代」。

### P1（建议同步修）

**Q4. 三处断链引用旧 09**（中）
`07:12`、`04:27`、`提示词模板/README.md:6` 的「09-调度控制平面与运行时台账规范」链接 → 统一改为 `09-Hermes调度与运行时台账规范`（含相对路径）。

**Q5. `04:321` 残留**（中）
`ExecutionThreadID` → `ExecutionRef`；「写入共享本地状态」→「由 Hermes 幂等写入台账」。

**Q6. `07:244-245` 质量门禁旧措辞**（中）
删「协调租约」，改「单实例 + `DispatchKey` 幂等去重」；`:245` Codex 措辞改「指定执行机制（WorkBuddy/Codex/Human）不被未经授权替代」。

### P2（可并入 V2.6 或批量修订）

**Q7. README 散点**（中）
`§5:223` 删「锁、租约」；`§7:260` 「系统集成 Reviewer」→「Reviewer（系统集成视角）」；`§8.2` `Pending` → `Planned`；`§2.9` 角色称谓统一。

**Q8. 模板文档头缺失 + AGENTS 状态未同步**（低）
01/02/03/04/05/06/08/09/10/11/12 模板补统一文档头（或与升级计划对齐一次性处理）；`AGENTS.md:44` 规范现状更新为「V2.5 待审核（V2.3 为上一已审核基线）」。

**Q9. 07 §2 角色列表未按 6 角色收敛**（低）
07 §2 增一句映射说明（System Reviewer→Reviewer、Main Merge→Integrator、Rework→Implementer、三审核→Doc/Design Reviewer，引用 README §4），或直接按 6 角色重写 §2 扩展列表。

---

## 12. 待解决/如实说明

- 本次验证为**静态审读**，未对断链链接逐个做工具化解析；3 处断链结论基于文件系统中 `09-调度控制平面与运行时台账规范.md` 已不存在（被 `git mv`）。
- 模板 01-12 是否补文档头，取决于 Hermes 是否将其列入后续批次（P3 已确认它们「零改动」）；若维持「零改动」，则文档头不一致为已知接受项。
- 未发现任何文件间「无法判断」的问题，所有条目均有明确文件/行号出处。

---

## 修订记录

| 版本 | 日期 | 作者 | 说明 |
|---|---|---|---|
| V1.0 | 2026-08-20 | WorkBuddy | P0–P3 全部批次完成后首次全库一致性验证；10 维度、9 项问题清单 |
