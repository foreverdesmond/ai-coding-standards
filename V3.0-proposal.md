# V3.0 规范修订提案：背景与建议

> 提案人：Hermes（Tiffany，总调度）
> 日期：2026-08-24
> 状态：草稿待审（Richy + Codex）
> 数据来源：SCRAPER-V5 迭代台账（89 条 Notes + Signals 全量）、RETROSPECTIVE.md、
>          2026-08-22 网关越权事故报告及整改实践

---

## 一、背景：V2.5 在 SCRAPER-V5 实战中的表现

V2.5 是规范库第一个完整经历多 Agent 大型迭代（DEV-001~006 + CTX 文档链 + L1/L2/L3 验证 +
集成合并）的版本。整体框架有效——任务拆分、上下文包、结构化协议头、push-before-review、
台账审计均发挥了作用。但实战暴露了三类问题：

### 1.1 规范与现实冲突，产生误导（必须勘误）

| 编号 | 冲突 | 实际影响 |
|---|---|---|
| C1 | 04 §362 `ExpectedExecutionKind` 静态枚举 vs 实际载体策略随额度/能力动态变化 | 载体选择依据三处不同步（口述/台账/文档），执行 Agent 无所适从 |
| C2 | 07 §24/§127、08 §30 "Reviewer 只读" vs 实测只读沙箱无法编译验证 | Review 出不了可信结论；Richy 已裁定 danger-full-access + 代码不可变约束，但文档未改 |

### 1.2 规范缺口：V5 踩坑无条款可依（需新增）

| 编号 | 事故/现象 | 缺失的规范 |
|---|---|---|
| G1 | 双驱动：cron 与交互会话并行消费同一信号、重复派发同一返工，RecordID 被复用，分支被未批准状态污染 | 调度权移交协议（互斥握手）、RecordID 全局唯一约束 |
| G2 | appsettings 孤本：主仓手工补配置→测试绿→判闭环；干净检出 L2 验证 6×MSB3030 再爆 | 干净检出可构建门禁；gitignore 依赖回退声明 |
| G3 | 豁免边界靠口头临时裁定；L2 曾差点把 MSB3030-appsettings 误归入签名豁免口径 | 豁免边界显式声明模板（精确到错误类型） |
| G4 | 执行故障与业务结论混淆：completed 但 finalResponse 仅"已发"/零业务结论 | 执行故障判定与消费禁令的明确条款 |

### 1.3 规范可发现性问题（长期方向）

05 §264 的"证据自指悖论"条款早已存在，CTX-002 仍然犯了两轮。
结论：**规范写了 ≠ Agent 会读到**。10 个文件 3362 行不可能常驻任何 Agent 的注意力。
方向：关键条款配套机械校验（如已上线的 dispatch_guard.py），让"遵守规范"从通读理解
变成过闸检查。

---

## 二、修订决议（Richy 已批准）

| 编号 | 决议 | 对应章节 |
|---|---|---|
| D1 | 新增《载体策略与变更控制》章：policy 文件为唯一真源、版本化、受控变更协议（Richy 宣布 → version++ → PolicyChange Signal → 勘误规范） | 09 新增 § |
| D2 | Reviewer 权限改为"代码不可变"约束；**所有 Codex 派发统一 danger-full-access**（含 Review——编译验证同样需要写权限） | 07、08 勘误 |
| D3 | 调度权移交协议：cron 与交互会话互斥握手四条（接管前核对尾部 tick / SHA 漂移停止上报 / push 回写台账 / RecordID 唯一） | 09 新增 § |
| D4 | RecordID 全局唯一：跨 Tasks+Signals 查重，违反即 BLOCKED | 09 勘误 |
| D5 | 验证环境同构门禁：L2+ 验证必须在干净 detached worktree 执行；gitignore 依赖须声明回退来源 | 08 新增 § |
| D6 | 豁免边界声明模板：随验证派发显式列出豁免项+理由，精确到错误类型 | 08 新增 § |
| D7 | 执行故障条款升格：completed 且无结构化头 = ExecutionFailure，补发或 PendingVerification，禁止消费 | 09 勘误 |
| D8 | 版本号定为 **V3.0**：Hermes 总调度模式经 V2.5 完整试用后的首个大版本 | 全局 |

## 三、机械门禁配套（超出文档层的落地物）

以下已在 SCRAPER-V5 收尾阶段实现并通过自测，V3.0 将其纳入规范引用：

| 组件 | 说明 | 状态 |
|---|---|---|
| `dispatch_guard.py` | 派发前置门禁引擎：8 项检查 fail-closed（字段完整/RecordID 唯一/载体可用/类型匹配策略/WB 单线程/调度暂停/依赖满足/SHA 可达）；selftest 3/3 | 已上线 |
| `carrier-policy.json` | 载体策略数据源（version 化，Richy 宣布变更 → version++ → 台账 PolicyChange Signal）；引擎 fail-closed 读它，规则变化不改代码 | v1 已建 |
| `codex_orchestration.py` guard hook | create 内嵌门禁：派发 JSON 无 guard 字段即拒（fail-closed），BLOCKED 不触网关 | 已上线 |
| `SOUL.md` 规则块 | 特权操作铁律 + policy gate 触发器（所有会话无条件注入） | 已生效 |

## 四、修订范围与交付物

| 文件 | 动作 | 内容 |
|---|---|---|
| 09-Hermes调度与运行时台账规范 | 新增两章 + 勘误 | D1 载体策略与变更控制 / D3 调度权移交 / D4 RecordID 唯一 / D7 执行故障升格 |
| 07-子Agent任务委派与提示词规范 | 勘误 | D2 Reviewer 权限改"代码不可变"；统一 danger-full-access |
| 08-开发验证、代码审查与合并门禁规范 | 新增两节 + 勘误 | D2 同步 / D5 干净检出门禁 / D6 豁免边界模板 |
| 04-详细开发任务文档规范 | 勘误 | C1 ExpectedExecutionKind 改为引用 policy 文件 |
| AGENTS.md | 版本头更新 | V3.0 基线声明 |
| 05-文档审核与变更控制规范 | 流程自身 | 本提案走 05 定义的审核流程 |

## 五、验收标准

1. 所有 conflictsWithDocs 条目清零（台账中记录的三项全部消除）；
2. dispatch_guard selftest 在新策略下全 PASS；
3. Richy + Codex 双方审核通过，V3.0 标记为正式基线；
4. 下一次迭代按新规范跑完首轮派发，guard 拦截率/合规率数据回填本仓库。

## 六、风险与开放问题

- R1: policy 文件与规范文本可能再次漂移 → 由 D1 的受控变更协议兜底（规范引用 policy，
  不复制 policy 内容）；
- R2: danger-full-access 全面放开扩大爆炸半径 → 以 SOUL.md 特权操作铁律 + OPS-AUDIT
  留痕 + 后续 systemd 用户级隔离（方案④）对冲；
- R3: 开放问题留给审核：D6 豁免边界的粒度（错误码级 or 类别级）？D3 移交协议是否需要
  Richy 电子签名式的确认动作？
