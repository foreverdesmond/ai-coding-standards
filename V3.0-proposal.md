# V3.0 规范修订提案：背景与建议

> 提案人：Hermes（Tiffany，总调度）
> 日期：2026-08-24（v1-v4）；2026-08-24（v5：终审前收口——D3 超时硬默认/D7 硬门槛/D2 隔离验证工作区/术语统一）
> 状态：v5 定稿候选（Richy + Codex 已附条件通过，四收口项已落实）
> v4 变更摘要：
>   ① 清除残留实现措辞：摘要字段统一为 PolicyArtifactDigest；制品引用统一为「载体策略制品契约」；§3 尾部具体台账名移除
>   ② §4-05 与 D1 冲突消除：契约/schema 变更走规范审核 vs 实例内容变更走受控运行时变更
>   ③ D3 补原子条件换主契约（Epoch+StateRevision 匹配才生效；派发消费校验 Epoch；状态随真源继承）
>   ④ D6 六项修正为完整七项（补「风险」）
>   ⑤ §7 证据要求升级为可独立复现标准（E1-E6），不可复现项须显式标记且不得作为定稿证据
> v3 变更摘要（对应 Codex 三项硬修订要求）：
>   ① 正文全部实现名/路径/命令已替换为制品类型与契约字段（§〇例示、D1、§3、§4-11、§7 重写）
>   ② D3 补 CoordinatorEpoch/FencingToken、失联超时接管（Richy 授权）、旧 Epoch 拒绝
>   ③ D7 补 TaskID+Stage+TargetIdentity 最大自动尝试上限 + NeedsAttention 升级出口
>   + D5 补受控清理出口与环境阻塞升级；D1 拆分契约变更 vs 实例变更两类变更通道
> v2 变更摘要：
>   - 总纲新增「规范定义契约 / 实例登记实现」分层原则（Richy 提出）
>   - D1-D8 全部按 Codex 审核意见加固（策略哈希/HEAD 对账/TransferID/ULID/粒度公式/取代关系）
>   - §4 修订范围扩展至 Codex 列出的全量文件（05/10/决议/清单/AGENTS/模板/policy schema）
>   - 新增 §7 门禁实现的验收证据要求（回应"无法独立核验 3/3 PASS"）
> 数据来源：SCRAPER-V5 迭代台账（89 条 Notes + Signals 全量）、RETROSPECTIVE.md、
>          2026-08-22 网关越权事故报告及整改实践

---

## 〇、修订总纲（v2 新增，Richy 定）：规范定义契约，不定义实现

规范回答「必须存在什么角色/机制、必须满足什么性质、如何验收」；不写具体脚本名、
本机路径或代码内容。三层分离：

| 层 | 内容 | 性质 | 存放 |
|---|---|---|---|
| 规范正文（normative） | 契约 + 性质 + 验收标准。例：「调度系统必须实现派发前置门禁：fail-closed；含 RecordID 全局唯一等检查项；每次派发记录 PolicyVersion 与 PolicyArtifactDigest；附可复现自检与故障注入样例」 | 稳定，跨机器可移植 | 本仓库文档 |
| 实例登记（informative, 非规范性部署记录） | 当前部署用哪个「载体策略制品」「派发前置门禁实现」满足哪条要求，钉不可变摘要（PolicyArtifactDigest / 实现 commit）。具体文件名与路径只出现在这里 | 运行时事实，随实现演进随时更新 | 各实例运行台账 / HANDOFF |
| 验收证据（per-release） | 自测用例 + 失败样例 + 实现 commit 指针，证明契约被满足且可复现 | V3.0 定稿审核附件 | 提案 PR / 仓库附件 |

推论：Codex 指出「origin/3.0 无实现文件、无法核验 3/3 PASS」成立 —— 解法是把实现名从
规范正文全部移除（改为制品类型），实现指针只存在于实例台账；v3.0 正文中每条门禁条款
标注「验收证据要求」，定稿前由 Hermes 附可复现测试（见 §7）。

---

## 一、背景：V2.5 在 SCRAPER-V5 实战中的表现

V2.5 是规范库第一个完整经历多 Agent 大型迭代（DEV-001~006 + CTX 文档链 + L1/L2/L3 验证 +
集成合并）的版本。整体框架有效——任务拆分、上下文包、结构化协议头、push-before-review、
台账审计均发挥了作用。但实战暴露了三类问题：

### 1.1 规范与现实冲突，产生误导（必须勘误）

| 编号 | 冲突 | 实际影响 |
|---|---|---|
| C1 | 04 §362 `ExpectedExecutionKind` 静态枚举 vs 实际载体策略随额度/能力动态变化 | 载体选择依据三处不同步（口述/台账/文档），执行 Agent 无所适从 |
| C2 | 07 §24/§127、08 §30 "Reviewer 只读" vs 实测只读沙箱无法编译验证 | Review 出不了可信结论；Richy 已裁定 danger-full-access + 代码不可变约束，但文档未改 |

### 1.2 规范缺口：V5 踩坑无条款可依（需新增）

| 编号 | 事故/现象 | 缺失的规范 |
|---|---|---|
| G1 | 双驱动：cron 与交互会话并行消费同一信号、重复派发同一返工，RecordID 被复用，分支被未批准状态污染 | 调度权移交协议（互斥握手）、RecordID 全局唯一约束 |
| G2 | appsettings 孤本：主仓手工补配置→测试绿→判闭环；干净检出 L2 验证 6×MSB3030 再爆 | 干净检出可构建门禁；gitignore 依赖回退声明 |
| G3 | 豁免边界靠口头临时裁定；L2 曾差点把 MSB3030-appsettings 误归入签名豁免口径 | 豁免边界显式声明模板（精确到错误类型） |
| G4 | 执行故障与业务结论混淆：completed 但 finalResponse 仅"已发"/零业务结论 | 执行故障判定与消费禁令的明确条款 |

### 1.3 规范可发现性问题（长期方向）

05 §264 的"证据自指悖论"条款早已存在，CTX-002 仍然犯了两轮。
结论：**规范写了 ≠ Agent 会读到**。10 个文件 3362 行不可能常驻任何 Agent 的注意力。
方向：关键条款配套机械校验（「派发前置门禁」类制品，见 §〇 三层分离与实例台账登记），
让"遵守规范"从通读理解变成过闸检查。

---

## 二、修订决议（D1-D8，v2 按 Codex 审核加固）

| 编号 | 决议 | v2 加固（Codex 意见吸收） |
|---|---|---|
| **D0** | **抽象化总纲**：规范定义契约与验收标准；实现登记于实例台账并钉 commit；两者以验收证据关联 | Richy 提出，见 §〇 |
| **D1** | 载体策略唯一真源：**载体策略制品**（PolicyArtifact）版本化 + 受控变更协议 | 加固：每次派发台账记录 **PolicyVersion + PolicyArtifactDigest**（不可变摘要）——仅记 version 无法发现同版本篡改。变更分两类（Codex 拆分，防 C1 复发）：<br>· **契约/schema 变化**（改字段、授权模型、fail-closed 语义、验收逻辑）→ 必须走规范变更审核<br>· **实例内容变化**（载体暂不可用、优先级、额度）→ 受控运行时变更：记授权人/PolicyVersion/Digest/生效范围即可，**不走文档审核**（否则日常调整被阻塞，重新引入 C1） |
| **D2** | Reviewer 权限改为「代码不可变」；所有 Codex 派发统一 danger-full-access（含 Review） | 加固：①Review 必须使用**隔离的、以精确候选 commit 为基线的验证工作区**（detached worktree），保护开发者原 worktree 的未提交内容与 Review 独立性；②**前后双向对账**——开始核验候选 HEAD/tree，结束再次核验 HEAD 未变且验证工作区无受跟踪业务源码 diff；一旦改动，本次 Review 结论无效。范围扩展：07/08/09(§211) + 公共基座/Coding Reviewer/系统 Reviewer/最终合并审核模板同步勘误 |
| **D3** | 调度权移交协议（cron ↔ 交互会话互斥）| 升格：推翻 V2.5「单机单实例、无协调租约锁」（09:38，及 §12.2:346 / 能力清单 / 总调度模板同源表述）的并发假设，作为**新并发控制模型**重写。协议要素：TransferID、旧/新调度身份、冻结点、台账 StateRevision、尾部 Signal 清单、双方确认回执、回滚条件。<br>**活性保证（v3 新增，Codex 指出失联卡死风险）**：<br>· 引入 **CoordinatorEpoch（FencingToken）**：任一时刻仅一个当前所有者；旧 Epoch 的任何派发/消费一律拒绝<br>· 正常移交 = 双确认回执<br>· 旧实例失联 → 超过 **LostOwnerTimeout**（固定默认值：2 个调度周期；项目可在策略制品中配置正整数阈值，派发前由门禁校验其存在且为正整数，未配置即用默认值）后进入**恢复模式**：由 Richy 明确授权新实例接管，禁止自动自愈<br>· 恢复接管后旧 Epoch 全部在途执行冻结待人工裁定<br>**原子换主（v4 新增，Codex 指出双主竞态未封死）**：<br>· 当前 Epoch、owner 身份与状态版本持久化于**调度状态真源**（台账）<br>· 接管必须经一次**原子条件更新**完成：仅当「当前 Epoch 与 StateRevision 仍匹配预期」时新 Epoch 才生效——条件不满足即接管失败重试<br>· 所有派发与消费动作校验当前 Epoch，不匹配即拒绝<br>· 尝试计数、暂停状态、待消费记录随该状态真源一并继承 |
| **D4** | RecordID 全局唯一 | 加固：生成方式定为**不可复用 ULID/UUID**（禁手工序号复用）；冲突处置 = 冻结冲突记录 + 生成新 ID + 保留映射审计，而非仅查重拒绝 |
| **D5** | 验证环境同构门禁：L2+ 必须干净 detached worktree | 加固：验证「精确候选 commit」；候选的**受跟踪业务源代码**不得变化；测试后受控清理测试产物（允许），工作区对受跟踪文件保持干净。<br>**可恢复出口（v3 新增，Codex 指出死循环风险）**：找不到受控配置回退来源时 → 形成明确的「环境/配置阻塞」记录 + 责任人 + 升级 Richy，**不得持续盲目重试** |
| **D6** | 豁免边界显式声明 | 粒度公式（v4 修正为完整七项，Codex 指出 v3 实为六项）：`命令/场景 + 失败签名或缺失依赖 + 影响范围 + **风险** + 替代证据 + 授权人 + 到期候选身份`。通用错误码（如 MSB3030）单独豁免过宽，禁止。实现上**扩展 08 §9 NotRun/例外既有机制**（其责任人/计划时间字段保留为模板字段，不混入边界匹配元组），不另起平行豁免体系 |
| **D7** | 执行故障条款升格 | 取代关系显式化：09:245「补发→PendingVerification」由本条**覆盖并收编**——缺结构化头的 completed 立即标 `ExecutionFailure/PendingVerification`，不得推进业务状态；原载体限次补发（≤1 次）。<br>**防无限循环（v3 新增，v4 收口为硬门槛）**：派发时必须记录 `MaxAutomaticAttempts`（**默认 3、不得超过 3**，含首次），计数持久化于调度状态真源——替换执行实例或调度权移交均不得重置。按 `TaskID + Stage + TargetIdentity` 计数达上限后转 `NeedsAttention` 并升级 Richy 人工处置，禁止自动无限重派 |
| **D8** | 版本号 V3.0 | 维持：调度一致性 + 权限模型 + 验证环境三项实质调整，非补丁级 |

## 三、机械门禁配套（实现层——按 D0 原则，正文只述制品类型与契约；实现指针登记于实例台账）

| 制品类型（规范语汇） | 满足的契约条款 | 契约要点 |
|---|---|---|
| **载体策略制品**（Carrier Policy Artifact） | §X.1 | version 化 + PolicyArtifactDigest + authorized_by 留痕；契约/schema 变更走规范审核，实例内容变化走受控运行时变更 |
| **派发前置门禁实现**（Dispatch Gate Implementation） | §X.2 | fail-closed；检查项至少含：意图字段完整、RecordID 全局唯一、载体可用且匹配策略、单线程约束、调度暂停闸、依赖满足、候选 SHA 可达；附「可执行自检入口」与故障注入样例 |
| **派发通道拦截器**（Dispatch Channel Interceptor） | §X.2 | 拦截器位于派发通道内：无门禁意图的派发请求即拒（不触执行端）；放行时响应附带 PolicyVersion 审计痕迹 |
| **规则注入制品**（Rule Injection Artifact） | 特权操作铁律 + 门禁触发器 | 对所有 agent 会话无条件注入 |

> 上述制品的当前实现名、代码 commit 与部署路径属**非规范性部署记录**，
> 登记于对应实例的运行台账，不在本规范正文出现。

## 四、修订范围（v2 按 Codex 意见全量扩展）

| # | 文件 | 动作 | 对应决议 |
|---|---|---|---|
| 1 | 09-Hermes调度与运行时台账规范 | 重写并发模型章 + 新增两章 + 勘误 | D1 载体策略章 / D3 调度权移交(取代 09:38 单实例假设) / D4 RecordID / D7 执行故障(取代 09:245) / 09:211 只读行勘误 |
| 2 | 08-开发验证、代码审查与合并门禁规范 | 新增两节 + 勘误 | D2 Reviewer 权限(08:30) / D5 干净检出门禁 / D6 豁免边界(**扩展 §9 NotRun/例外既有机制**) |
| 3 | 07-子Agent任务委派与提示词规范 | 勘误 | D2 (§24/§127)；ExpectedExecutionKind 静态枚举改引用载体策略制品契约 |
| 4 | 04-详细开发任务文档规范 | 勘误 | C1 ExpectedExecutionKind(§362) 静态枚举改为引用「载体策略制品契约」 |
| 5 | 05-文档审核与变更控制规范 | 扩展 | 增加「外部契约制品」变更控制规则，**与 D1 拆分对齐**：策略制品的**契约/schema 变更**走规范变更审核；**实例内容变更**走受控运行时变更（授权人+PolicyVersion+Digest+生效范围），不走规范审核 |
| 6 | 10-术语表 | 新增术语 | PolicyVersion / TransferID / ExecutionFailure / 豁免边界七项组 / 代码不可变约束 |
| 7 | Hermes流程与边界决议.md | 同步 | D3 并发控制模型变化 |
| 8 | Hermes能力边界清单.md | 同步 | danger-full-access 统一适用（含 Review）；跨 profile 服务隔离铁律 |
| 9 | AGENTS.md | 版本头更新 | V3.0 基线声明 + 引用 policy gate |
| 10 | 提示词模板/ | 同步 | 公共基座、总调度、Coding Reviewer、系统 Reviewer、最终合并审核、验证 Agent 六类模板的权限与门禁表述 |
| 11 | 载体策略制品 schema + 变更记录 | 新增（登记于实例台账，钉不可变摘要） | D1 配套：schema 定义、变更历史、门禁自检样例与失败样例（见 §7）；**schema 契约变化走规范审核，实例内容变化走运行时变更**（D1 拆分） |

## 五、验收标准

1. 所有 conflictsWithDocs 条目清零（台账中记录的三项全部消除）；
2. 规范正文零实现路径、零本机文件名（D0 抽象化总纲达成，由 Richy/Codex 抽查确认）；
3. 门禁实现的可复现证据包提交（见 §7），Codex 可独立复跑「可执行自检入口」并构造故障注入样例验证 fail-closed；
4. D3 移交协议在下一迭代实际演练一次（cron↔交互会话移交各一次），TransferID 留痕台账；
5. Richy + Codex 双方审核通过，V3.0 标记为正式基线；
6. 下一次迭代按新规范跑完首轮派发，guard 拦截率/合规率数据回填本仓库。

## 六、风险与开放问题

- R1: 载体策略制品与规范文本可能再次漂移 → 由 D1 受控变更协议兜底（规范只引用
  「载体策略制品契约」，不复制制品内容）；
- R2: danger-full-access 全面放开扩大爆炸半径 → 以「规则注入制品」中的特权操作铁律 +
  OPS-AUDIT 留痕 + 后续 systemd 用户级隔离（方案④）对冲；
- R3: ~~开放问题：D6 豁免粒度~~ 已解决：采纳完整七项粒度公式；
- R4: danger-full-access 含 Review 后，Reviewer 误改代码的风险 → 以 D2 双向 HEAD/tree 对账 +
  「改动即结论无效」对冲；
- R5: 载体策略制品本身成为攻击面（篡改内容而 version 不变）→ D1 的 PolicyArtifactDigest 记录 +
  门禁启动时校验对冲；
- 开放问题（留给定稿审核）：D3 移交协议是否需要 Richy 显式确认动作作为 TransferID 生效条件？

---

## 七、门禁实现的验收证据要求（v3：按制品类型表述，实现指针只入实例台账）

V3.0 定稿前，Hermes 必须提交以下**可复现证据包**；每项附「派发前置门禁实现」与
「载体策略制品」的不可变摘要，登记于实例台账（非规范性部署记录）：

| # | 证据项 | 契约依据 |
|---|---|---|
| E1 | **实现来源**：审查者可访问的不可变实现来源或封存制品（公开仓库 commit / 附件封存），而非仅某台机器的本机路径 | D0 实例登记 |
| E2 | **不可变摘要与算法**：每个制品的 Digest 及其算法名（如 sha256），供独立核验一致性 | D0/D1 |
| E3 | **可执行自检入口**：调用前提、依赖版本、预期结果三要素齐备的全量输出 | §X.2 自检契约 |
| E4 | **故障注入样例 ≥3**：①对已闭环任务重复派发（依赖闸拦截）②RecordID 复用（唯一性拦截）③调度暂停态派发（暂停闸拦截）——各附注入输入与完整原始输出 | §X.2 各检查项 |
| E5 | **fail-closed 反证**：载体策略制品缺失版本字段/结构损坏时，门禁必须拒绝而非放行 | fail-closed 契约 |
| E6 | **拦截器反证**：无门禁意图的派发请求被拒的原始输出 | 拦截器契约 |
| E7 | **D3 反证①**：旧 Epoch 在接管后尝试派发/消费 → 必须被拒绝的原始输出 | D3 FencingToken 契约 |
| E8 | **D3 反证②**：两个候选接管者并发尝试原子换主 → 仅一个成功、另一个条件更新失败的原始输出 | D3 原子换主契约 |

任一项无法满足审查者可访问性时，该项必须显式标记「**不可独立复现**」，
不得作为 V3.0 定稿通过的证据。以上任一必选项缺失，V3.0 不得进入定稿审核。
