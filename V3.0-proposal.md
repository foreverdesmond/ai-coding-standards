# V3.0 规范修订提案：背景与建议

> 提案人：Hermes（Tiffany，总调度）
> 日期：2026-08-24（v1）；2026-08-24（v2，吸收 Codex 审核意见与 Richy 抽象化原则）
> 状态：v2 草稿待审（Richy + Codex）
> v2 变更摘要：
>   - 总纲新增「规范定义契约 / 实例登记实现」分层原则（Richy 提出）
>   - D1-D8 全部按 Codex 审核意见加固（策略哈希/HEAD 对账/TransferID/ULID/粒度公式/取代关系）
>   - §4 修订范围扩展至 Codex 列出的全量文件（05/10/决议/清单/AGENTS/模板/policy schema）
>   - 新增 §7 门禁实现的验收证据要求（回应"无法独立核验 3/3 PASS"）
> 数据来源：SCRAPER-V5 迭代台账（89 条 Notes + Signals 全量）、RETROSPECTIVE.md、
>          2026-08-22 网关越权事故报告及整改实践

---

## 〇、修订总纲（v2 新增，Richy 定）：规范定义契约，不定义实现

规范回答「必须存在什么角色/机制、必须满足什么性质、如何验收」；不写具体脚本名、
本机路径或代码内容。三层分离：

| 层 | 内容 | 性质 | 存放 |
|---|---|---|---|
| 规范正文（normative） | 契约 + 性质 + 验收标准。例：「调度系统必须实现派发前置门禁：fail-closed；含 RecordID 全局唯一等检查项；每次派发记录 PolicyVersion 与策略文件哈希；附可复现自测与失败样例」 | 稳定，跨机器可移植 | 本仓库文档 |
| 实例登记（informative） | 当前部署用哪个实现满足哪条要求，钉 commit SHA。例：「dispatch_guard.py @ <sha> 满足 §X.2」 | 运行时事实，随实现演进随时更新 | 各实例运行台账 / HANDOFF |
| 验收证据（per-release） | 自测用例 + 失败样例 + 实现 commit 指针，证明契约被满足且可复现 | V3.0 定稿审核附件 | 提案 PR / 仓库附件 |

推论：Codex 指出「origin/3.0 无实现文件、无法核验 3/3 PASS」成立 —— 解法不是把 py/json
塞进规范，而是 v3.0 正文中每条门禁条款都标注「验收证据要求」，定稿前由 Hermes 附上
实现指针 + 可复现测试（见 §7）。

---

## 一、背景：V2.5 在 SCRAPER-V5 实战中的表现

V2.5 是规范库第一个完整经历多 Agent 大型迭代（DEV-001~006 + CTX 文档链 + L1/L2/L3 验证 +
集成合并）的版本。整体框架有效——任务拆分、上下文包、结构化协议头、push-before-review、
台账审计均发挥了作用。但实战暴露了三类问题：

### 1.1 规范与现实冲突，产生误导（必须勘误）

| 编号 | 冲突 | 实际影响 |
|---|---|---|
| C1 | 04 §362 `ExpectedExecutionKind` 静态枚举 vs 实际载体策略随额度/能力动态变化 | 载体选择依据三处不同步（口述/台账/文档），执行 Agent 无所适从 |
| C2 | 07 §24/§127、08 §30 "Reviewer 只读" vs 实测只读沙箱无法编译验证 | Review 出不了可信结论；Richy 已裁定 danger-full-access + 代码不可变约束，但文档未改 |

### 1.2 规范缺口：V5 踩坑无条款可依（需新增）

| 编号 | 事故/现象 | 缺失的规范 |
|---|---|---|
| G1 | 双驱动：cron 与交互会话并行消费同一信号、重复派发同一返工，RecordID 被复用，分支被未批准状态污染 | 调度权移交协议（互斥握手）、RecordID 全局唯一约束 |
| G2 | appsettings 孤本：主仓手工补配置→测试绿→判闭环；干净检出 L2 验证 6×MSB3030 再爆 | 干净检出可构建门禁；gitignore 依赖回退声明 |
| G3 | 豁免边界靠口头临时裁定；L2 曾差点把 MSB3030-appsettings 误归入签名豁免口径 | 豁免边界显式声明模板（精确到错误类型） |
| G4 | 执行故障与业务结论混淆：completed 但 finalResponse 仅"已发"/零业务结论 | 执行故障判定与消费禁令的明确条款 |

### 1.3 规范可发现性问题（长期方向）

05 §264 的"证据自指悖论"条款早已存在，CTX-002 仍然犯了两轮。
结论：**规范写了 ≠ Agent 会读到**。10 个文件 3362 行不可能常驻任何 Agent 的注意力。
方向：关键条款配套机械校验（如已上线的 dispatch_guard.py），让"遵守规范"从通读理解
变成过闸检查。

---

## 二、修订决议（D1-D8，v2 按 Codex 审核加固）

| 编号 | 决议 | v2 加固（Codex 意见吸收） |
|---|---|---|
| **D0** | **抽象化总纲**：规范定义契约与验收标准；实现登记于实例台账并钉 commit；两者以验收证据关联 | Richy 提出，见 §〇 |
| **D1** | 载体策略唯一真源：policy 文件版本化 + 受控变更协议 | 加固：每次派发台账记录 **PolicyVersion + 策略文件 SHA-256**——仅记 version 无法发现同版本篡改 |
| **D2** | Reviewer 权限改为「代码不可变」；所有 Codex 派发统一 danger-full-access（含 Review） | 加固：Review **前后双向对账**——审查开始核验候选 HEAD/tree，结束再次核验 HEAD 未变且工作区无业务代码 diff；一旦改动，本次 Review 结论无效。范围扩展：07/08/09(§211) + 公共基座/Coding Reviewer/系统 Reviewer/最终合并审核模板同步勘误 |
| **D3** | 调度权移交协议（cron ↔ 交互会话互斥） | 升格：这推翻 V2.5「单机单实例、无协调租约锁」（09:38）的并发假设，须作为**新并发控制模型**重写而非附加流程。协议要素：TransferID、旧/新调度身份、冻结点、台账 StateRevision、尾部 Signal 清单、双方确认回执、回滚条件。范围扩展：《Hermes流程与边界决议》《Hermes能力边界清单》同步 |
| **D4** | RecordID 全局唯一 | 加固：生成方式定为**不可复用 ULID/UUID**（禁手工序号复用）；冲突处置 = 冻结冲突记录 + 生成新 ID + 保留映射审计，而非仅查重拒绝 |
| **D5** | 验证环境同构门禁：L2+ 必须干净 detached worktree | 加固：明确验证「精确候选 commit」；测试前后工作区均干净；被忽略配置必须有**可复现受控回退来源**（如 sample 回退）；密钥不得进入证据 |
| **D6** | 豁免边界显式声明 | 粒度公式（Codex 版）：`命令/场景 + 具体失败签名或缺失依赖 + 影响范围 + 替代证据 + 授权人 + 到期候选 SHA` 七元组。通用错误码（如 MSB3030）单独豁免过宽，禁止。实现上**扩展 08 §9 NotRun/例外既有机制**（风险/责任人/批准人字段已具备），不另起平行豁免体系 |
| **D7** | 执行故障条款升格 | 取代关系显式化：09:245「补发→PendingVerification」由本条**覆盖并收编**——缺结构化头的 completed 立即标 `ExecutionFailure/PendingVerification`，不得推进业务状态；原载体限次补发（≤1 次），超限拒绝并重派新执行 |
| **D8** | 版本号 V3.0 | 维持：调度一致性 + 权限模型 + 验证环境三项实质调整，非补丁级 |

## 三、机械门禁配套（实现层，登记于实例台账）

以下已在 SCRAPER-V5 收尾阶段实现并通过自测。**按 D0 原则，规范正文只引用其满足的契约条款；
实现本体与 commit 指针登记于实例台账，验收证据见 §7。**

| 组件（实现名） | 满足的契约条款 | 状态 |
|---|---|---|
| dispatch_guard.py | §X.2 派发前置门禁：fail-closed；8 项检查（字段完整/RecordID 全局唯一/载体可用/类型匹配策略/WB 单线程/调度暂停/依赖满足/SHA 可达）；selftest 3/3 | 已上线 |
| carrier-policy.json + schema | §X.1 载体策略唯一真源：version 化、受控变更协议、authorized_by 留痕 | v1 已建 |
| codex_orchestration.py guard hook | §X.2 物理拦截：派发 JSON 无 guard 意图即拒（不触网关）；APPROVED 响应附 policy_version 审计痕迹 | 已上线 |
| SOUL.md 规则块 | 特权操作铁律 + policy gate 触发器（所有会话无条件注入） | 已生效 |

## 四、修订范围（v2 按 Codex 意见全量扩展）

| # | 文件 | 动作 | 对应决议 |
|---|---|---|---|
| 1 | 09-Hermes调度与运行时台账规范 | 重写并发模型章 + 新增两章 + 勘误 | D1 载体策略章 / D3 调度权移交(取代 09:38 单实例假设) / D4 RecordID / D7 执行故障(取代 09:245) / 09:211 只读行勘误 |
| 2 | 08-开发验证、代码审查与合并门禁规范 | 新增两节 + 勘误 | D2 Reviewer 权限(08:30) / D5 干净检出门禁 / D6 豁免边界(**扩展 §9 NotRun/例外既有机制**) |
| 3 | 07-子Agent任务委派与提示词规范 | 勘误 | D2 (§24/§127)；ExpectedExecutionKind 静态枚举改引用 policy |
| 4 | 04-详细开发任务文档规范 | 勘误 | C1 ExpectedExecutionKind(§362) 改引用 policy 文件 |
| 5 | 05-文档审核与变更控制规范 | 扩展 | 增加「规范引用外部契约文件」的变更控制规则（policy 变更视同规范勘误走审核） |
| 6 | 10-术语表 | 新增术语 | PolicyVersion / TransferID / ExecutionFailure / 豁免边界七元组 / 代码不可变约束 |
| 7 | Hermes流程与边界决议.md | 同步 | D3 并发控制模型变化 |
| 8 | Hermes能力边界清单.md | 同步 | danger-full-access 统一适用（含 Review）；跨 profile 服务隔离铁律 |
| 9 | AGENTS.md | 版本头更新 | V3.0 基线声明 + 引用 policy gate |
| 10 | 提示词模板/ | 同步 | 公共基座、总调度、Coding Reviewer、系统 Reviewer、最终合并审核、验证 Agent 六类模板的权限与门禁表述 |
| 11 | carrier-policy schema + 变更记录 | 新增（登记于实例台账，钉 commit） | D1 配套：schema 定义、变更历史、guard 验收脚本与失败样例（见 §7） |

## 五、验收标准

1. 所有 conflictsWithDocs 条目清零（台账中记录的三项全部消除）；
2. 规范正文零实现路径、零本机文件名（D0 抽象化总纲达成，由 Richy/Codex 抽查确认）；
3. 门禁实现的可复现证据包提交（见 §7），Codex 可独立复跑 selftest 并构造失败样例验证 fail-closed；
4. D3 移交协议在下一迭代实际演练一次（cron↔交互会话移交各一次），TransferID 留痕台账；
5. Richy + Codex 双方审核通过，V3.0 标记为正式基线；
6. 下一次迭代按新规范跑完首轮派发，guard 拦截率/合规率数据回填本仓库。

## 六、风险与开放问题

- R1: policy 文件与规范文本可能再次漂移 → 由 D1 的受控变更协议兜底（规范引用 policy，
  不复制 policy 内容）；
- R2: danger-full-access 全面放开扩大爆炸半径 → 以 SOUL.md 特权操作铁律 + OPS-AUDIT
  留痕 + 后续 systemd 用户级隔离（方案④）对冲；
- R3: ~~开放问题：D6 豁免粒度~~ 已解决：采纳 Codex 七元组公式；
- R4: danger-full-access 含 Review 后，Reviewer 误改代码的风险 → 以 D2 双向 HEAD/tree 对账 +
  「改动即结论无效」对冲；
- R5: policy 文件本身成为攻击面（篡改 version 不变内容）→ D1 加固的策略 SHA-256 记录 +
  guard 启动校验对冲；
- 开放问题（留给定稿审核）：D3 移交协议是否需要 Richy 显式确认动作作为 TransferID 生效条件？

---

## 七、门禁实现的验收证据要求（v2 新增，回应 Codex「无法独立核验」）

V3.0 定稿前，Hermes 必须提交以下可复现证据包（登记于实例台账并钉 commit）：

1. **实现指针**：dispatch_guard.py / carrier-policy.json / codex_orchestration.py(hook) 的
   当前完整 SHA 与获取方式（本机路径 + 内容哈希；如后续迁移入仓库则直接钉 commit）；
2. **selftest 全量输出**：`dispatch_guard.py selftest` 原始 stdout（含 PASS 判定依据）；
3. **失败样例**：至少 3 个真实案例回测——①误派已闭环任务复核（双理由拦截）
   ②RecordID 复用（幂等键击穿拦截）③Paused 状态下派发（暂停闸拦截）——每例附
   BLOCKED reasons 全文；
4. **fail-closed 反证**：损坏 policy 文件（删 version 字段）后 guard 必须拒绝而非放行的演示；
5. **hook 反证**：create 无 guard 字段的派发 JSON 被拒的原始输出。

以上任一项缺失或不可复现，V3.0 不得进入定稿审核。
