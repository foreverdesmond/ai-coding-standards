# 大纲审阅报告（规范总审阅 · WorkBuddy 担任）

> 状态：草稿（供 Hermes 总调度审阅）
> 审阅人：WorkBuddy（规范总审阅角色）
> 审阅对象：ai-coding-standards V2.4 全量规范 + V2.5 分支三份调研大纲
> 日期：2026-08-20
> 核心理念（审阅基线）：Hermes 常驻服务做总调度，不代执行子 Agent 的 Git 与开发职责；WorkBuddy/Codex 平行协作执行。

---

## 第一部分 · 全量规范审核结论（V2.4 → Hermes 模式）

### 1.1 结论总览

V2.4 是一套**分层清晰、状态机严谨**的多 Agent 开发规范。其"文档链（背景→需求→设计→任务→上下文）"与"验证门禁（Level 0–3 + Git 权限矩阵 + 状态机）"是**跨执行载体通用的成熟资产**，与调度机制无关，全部保留。真正需要动的是**调度控制层（09）与执行机制层（07 的枚举、00/07/13/14/15 模板）**——它们深度绑定了"Codex 线程当总调度 + 共享本地 JSON + 轮询巡检"这套 V2.4 专为 Codex 量身定做的实现。

判定口诀：**状态机语义全保留，Codex 线程实现全裁剪，Hermes 原生能力做替代，缺失的执行/上下文规范补漏。**

### 1.2 合理可保留（与调度机制无关）

| 对象 | 结论 |
|---|---|
| 文档规范 01 背景 / 02 需求 / 03 设计（V2.3 已审核） | **全部保留**。纯文档工程规范，无任何 Codex 调度依赖。仅各文档 §「Codex 执行要求」章节需把"Codex"一词泛化为"执行 Agent（WorkBuddy/Codex）"。 |
| 文档规范 06 上下文包（V2.3 已审核） | **保留**。Context L1/L2/L3 分层、触发条件、失效规则通用。注意：正文沿用旧名 `BaseSHA`，需统一为 `CodeBaseSHA`（05 已定义兼容名，但 06 未同步）。 |
| 文档规范 08 验证/审查/合并门禁 | **保留主体**。Level 0–3 定义、Review 执行状态与 Verdict 分离、Git 权限边界、破坏性资源安全、NotRun 规则全部通用。仅 §3.2.1 中"控制面执行故障以 09 为准"的引用需随 09 重写同步。 |
| 提示词模板 01 开发 / 02 Reviewer / 03 返工 / 04 测试 / 05 系统集成 Reviewer / 06 最终合并审核 / 08 需求审核 / 09 设计审核 / 11 迭代集成 / 12 主分支合并 | **保留角色契约骨架**。角色目标、输出协议头、权限边界通用。其中 01/02/03 尾部依赖 `StateUpdateToolPath` 发布状态，需改为"Hermes 台账写入接口"；04/05/06/08/09/11/12 相对独立，仅需统一身份字段名。 |
| README 核心资产 | 角色矩阵、三档风险路线、阶段地图、默认分支映射、Git 权限矩阵（§6.1）、验证等级概览（§7）、状态语义（§8）、V2.3 已审核决定（§12）**全部保留**。 |
| 核心原则 2.1–2.9 | **保留**。事实优先、上游定边界、局部正确≠系统正确、上下文地图、独立审查、验证分层、安全优先、SOLID 硬门禁、职责分离，与所有开源最佳实践一致。 |

### 1.3 必须裁剪（Codex 线程专属实现）

| 裁剪对象 | 所在位置 | 理由 |
|---|---|---|
| 共享本地 JSON 唯一真源（`SharedRuntimeStatePath` + 短时锁 + 原子更新工具 + `StateUpdateToolPath`） | 09 §5.1、04 §4/§8、07 §8、00 公共基座、07/13/14/15 模板 | 这是在 Codex 线程里用文件锁模拟调度器的实现；Hermes 自带持久化 + 单实例，用台账替代 |
| 协调租约锁（`TaskStateWriteLockPath`、租约字段） | 09 §12、00/07/13 模板 | 单实例 Hermes 无多 Coordinator 竞争，幂等由 DispatchKey 去重保证 |
| 周期巡检作为"独立任务"（`InspectionMode: SharedLocalStatePolling + Watchdog`） | 09 §11、13 模板 | Hermes 常驻，巡检是调度循环内置步骤，不是独立可重复任务 |
| 冷恢复作为"独立任务"（RecoveryOnly 从 JSON 重建） | 09 §13、14 模板 | Hermes 重启读台账快照 + Git，天然恢复 |
| `ExpectedExecutionKind: CodexThread / SubAgent` | 07 §2.1、04、README | Codex 专属枚举，改为 WorkBuddy/Codex/Human/ApprovedEquivalent |
| "创建 Codex 子任务失败不得用 spawn_agent 兜底" | 09 §7-10、07 §2.1、00/07 模板 | 绑定 Codex 任务系统，退回"禁止伪独立自审"精神 |
| `clientThreadId → threadId` 两段式映射 | 09 §3.1 | 统一为 `ExecutionRef`/SessionID |
| `CoordinatorThreadID`（Coordinator 作为线程） | 09、07 模板 | Hermes 是常驻服务身份，不是某次线程 |

### 1.4 必须修改（保留语义，适配 Hermes）

| 修改对象 | 修改方向 |
|---|---|
| 三类状态分离（ThreadStatus/TaskState/EvidenceState） | **保留**；ThreadStatus 语义从"线程状态"泛化为"执行载体状态" |
| 幂等 `DispatchKey` | **保留**；明确 `DispatchKey = IterationID + TaskID + Stage + TargetIdentity`（`InvocationID` 每次唯一，不能当去重键） |
| 证据消费状态机（Unread→…→Consumed）、`RecordID+SignalRevision` 幂等消费 | **保留** |
| 最大安全状态恢复 | **保留**；落点改为"Hermes 重启读台账 + Git 重放" |
| Review 执行状态与 Verdict 分离、NotRun 语义 | **保留** |
| 核心原则 2.10/2.11 | **改写表述**：保留"文档状态驱动、状态分离、持久可恢复可审计"精神，删"共享 JSON/StateUpdateToolPath/轮询"实现细节 |
| 提示词组装（07 §4） | 动态数据改由 Hermes 注入 |
| 依赖推进（07 §8） | Hermes 台账记依赖图，满足即主动触发下游 |
| 角色独立性（07 §5） | 载体分离（WB 写/Codex 审）＋独立风险模型，载体分离 ≠ 视角独立 |
| Finding 三次升级（07 §8.4） | 保留"第2轮分类根因→第3轮停机械返工上报裁决"，计数只是触发手段 |

### 1.5 必须补漏（V2.4 缺失）

1. **Hermes 能力边界清单**：cron 配置、事件来源（飞书/webhook）、台账存储位置、Agent 派发 API、工具权限。一切重构的前提。
2. **Context L1 标准入口**：AGENTS.md 式分层（global/项目级/子包级 + closest-wins 优先级）。
3. **执行机制适配层**：统一 `ExecutionRef`，Hermes ↔ WorkBuddy/Codex/Human 的会话协议。
4. **运行时工具权限边界**：只读/读写/危险三级，与 `ExpectedExecutionKind` 关联。
5. **证据落盘规范**：台账只存指针，长结果/证据写入派生证据目录。
6. **派发侧输入校验（guardrail 双侧）**：09 已有消费侧校验，缺派发前输入校验。
7. **事件加速通道**：事件到达立即处理 + cron 轮询兜底，事件丢失不得影响正确性。
8. **00-公共基座重写**：耦合 SharedRuntimeStatePath 等 Codex 字段最重，改为 Hermes 注入协议。

---

## 第二部分 · 三份调研大纲逐项审阅

### 2.1 审阅原则

以"Hermes 常驻服务做总调度、不代执行子 Agent 的 Git 和开发职责"为核心理念，结合第一部分我自己的全量审核结论交叉验证。每项判：**合理 ✓ / 需商榷 ⚠️ / 不合理 ✗**。

---

### 2.2 《调研分析-WorkBuddy大纲.md》审阅

| 建议项 | 内容摘要 | 判断 |
|---|---|---|
| 保留五类资产 | 角色矩阵/风险路线/Level 0–3/Git权限/状态机/原则2.1–2.9/文档01-06,08 | ✓ 与我第一部分一致 |
| 裁剪 7 项 | CodexThread枚举、spawn_agent禁令、clientThreadId映射、共享JSON+锁+原子脚本、轮询巡检、租约锁、从JSON冷恢复 | ✓ 准确；唯"冷恢复"裁剪需补"Hermes 重启恢复"替代，原文未明示 |
| 补漏 5 方向 | Hermes能力边界清单、AGENTS.md分层、agent能力注册、结构化handoff、权限scope、失败信号 | ✓ 方向正确，但粒度偏粗（详见打分） |
| 重写 09/README§10/原则2.10-2.11/调度三件套+00基座 | — | ✓ 与第一部分一致，且补了 00 基座 |
| 修改 5 项 | 提示词组装/依赖推进/角色独立/Finding升级/幂等键 | ✓ 与工作稿 A/D/E 条一致 |
| 09/07/README 逐项判定 | 09机制层不适用、状态机层适用；07骨架适用；README主体适用 | ✓ 准确 |
| **缺失项** | 无优先级落地顺序、无术语映射表、无 Canary 常驻场景扩展 | ⚠️ 完整性短板 |

---

### 2.3 《调研分析-Codex-deepseek大纲.md》审阅

| 建议项 | 内容摘要 | 判断 |
|---|---|---|
| T-1 裁剪周期巡检"独立任务化" | 巡检改为常驻循环 | ✓ |
| T-2 裁剪 Codex 子任务创建细节 | 由会话适配层抽象 | ✓ |
| T-3 裁剪过度细化字段 | 对齐 subagent frontmatter 最小字段 | ⚠️ 字段是证据链可审计性的一部分，过度裁剪有风险 |
| T-4 裁剪 README §2.10 并列表述 | 调度者=巡检循环 | ✓ |
| G-1 AGENTS.md 入口 | closest-wins 优先级 | ✓ |
| G-2 常驻总调度生命周期 | 启动/热恢复/idle循环/多实例防重 | ✓ 独特增量，价值高 |
| G-3 证据内容落盘 | 状态只存指针，长证据落盘 | ✓ 独特增量，价值高 |
| G-4 guardrail 双侧校验 | 派发前输入校验 | ✓ 独特增量，价值高 |
| G-5 阶段摘要外部记忆 | 子任务写结构化摘要 | ✓ 独特增量，价值高 |
| G-6 config_change 通知语义 | 权限/模型变更事件 | ⚠️ 有价值但与单实例场景复杂度可能不成比例 |
| R-1 周期巡检→常驻调度循环 | — | ✓ |
| R-2 07§8→常驻总调度契约 | — | ✓ |
| M-1~M-5 字段/事件/双通道/Canary/README措辞 | — | ✓ 稳健 |
| **核心分歧：§2.2"共享 JSON + 轮询保留为兜底"** | 认为共享 JSON 仍是最优解 | ⚠️ 见第三部分冲突裁决 |

---

### 2.4 《调研分析-Codex-mimo大纲.md》审阅

| 建议项 | 内容摘要 | 判断 |
|---|---|---|
| C-1 裁剪共享JSON+文件锁+原子脚本 | — | ✓ |
| C-2 裁剪协调租约锁 | 单实例+DispatchKey去重 | ✓ |
| C-3 枚举改四档 | WorkBuddy/Codex/Human/ApprovedEquivalent | ✓ 吸收了工作稿 B 条纠错 |
| C-4 spawn_agent禁令退回精神 | 禁止伪独立自审 | ✓ |
| C-5 clientThreadId→ExecutionRef | — | ✓ |
| C-6 "不依赖聊天上下文"措辞 | 台账为真源 | ✓ |
| L-1~L-4 补漏 | AGENTS.md/能力清单/适配层/工具权限 | ✓ |
| L-5 并行派发与结果汇总 | — | ⚠️ 增强项而非必须，单实例下 V2.4 依赖图已够用 |
| L-6 事件加速通道 | — | ✓ |
| L-7 Finding 三次升级 | 保留根因分类 | ✓ |
| L-8 00基座重写 | — | ✓ |
| R-1/R-2/R-3 重写 | 09/07模板/00基座 | ✓ |
| M-1~M-7 修改 | 提示词组装/依赖推进/角色独立/Finding/幂等键/三件套/枚举 | ✓ 吸收了工作稿 A~E 条全部纠错 |
| **摘要与裁剪项轻微不一致** | §0 说"保留共享JSON唯一真源"，§2.2 说"删除共享JSON文件锁" | ⚠️ 表述瑕疵，精神一致（保留语义删实现） |

---

## 第三部分 · 冲突点与裁决

### 冲突 1：共享 JSON 的去留（WorkBuddy/mimo vs deepseek）

- **WorkBuddy/mimo**：共享 JSON 文件 + 文件锁 + 原子脚本是 Codex 时代实现，裁剪，改 Hermes 台账。
- **deepseek**：共享 JSON + 轮询保留为兜底通道，事件做加速。

**裁决**：采纳 **"保留语义、裁剪实现"** 的合并方案——
1. **三方一致保留**：唯一状态真源、`RecordID+SignalRevision` 幂等消费、定向读取、最大安全状态恢复——这些语义与载体无关，必留。
2. **裁剪**：共享 JSON 文件、短时锁、原子更新脚本、协调租约——Hermes 单实例 + 自带持久化，这些 Codex 线程模拟机制冗余。
3. **采纳 deepseek 的稳健性增量**："事件加速 + cron 轮询兜底"双通道——事件丢失不影响正确性。但兜底落点是 **Hermes cron 定期对账**，不是保留 Codex 的"共享 JSON 轮询巡检任务"。
4. **台账载体**（JSON 文件 vs SQLite）：不预判，交 Hermes 能力边界清单实测后定。载体无关，语义必留。

### 冲突 2：ACP 的定义分歧

- **WorkBuddy**：ACP = Agent Communication Protocol（agent↔agent 通信，Linux Foundation 2025）。
- **deepseek/mimo**：ACP = Agent Client Protocol（主机↔Agent 会话，JSON-RPC over stdio）。

**裁决**：两者是不同协议。**采纳 deepseek/mimo 的 Agent Client Protocol 定位**——Hermes 是 host、WorkBuddy/Codex 是 agent，主机↔Agent 会话生命周期（new→prompt→update→cancel）正好对应 Hermes 派发执行载体的场景。WorkBuddy 引用的"agent 能力注册"思想保留，但落点为"执行机制适配层"，而非对等 agent 通信。

### 冲突 3：补漏项粒度（5 vs 6 vs 8）

三方补漏项**互补不冲突**，合并采纳：WorkBuddy 的 5 个方向 + deepseek 的 G-2/G-3/G-4/G-5/G-6 + mimo 的 L-3/L-4/L-5/L-6，去重后形成完整补漏清单（见文档 B）。

---

## 第四部分 · 打分

| 维度（各20分） | WorkBuddy | deepseek | mimo |
|---|---:|---:|---:|
| 准确性（对 V2.4 理解） | 17 | 16 | 18 |
| 完整性（覆盖全面） | 14 | 17 | 18 |
| 可行性（可落地） | 15 | 15 | 17 |
| Hermes 调度适配度 | 17 | 15 | 18 |
| 结构清晰度 | 15 | 17 | 18 |
| **总分** | **78** | **80** | **89** |

**排序与理由**：

1. **Codex-mimo（89 分）**：综合最优。准确吸收了 Richy 工作稿 A~E 五条纠错（幂等键/枚举四档/00基座重写/角色独立/Finding根因），架构映射表与术语映射表让结论直接可落地，P0–P3 优先级清晰。唯一瑕疵是摘要与裁剪项的轻微表述不一致，以及 L-5 并行派发偏前瞻。
2. **Codex-deepseek（80 分）**：有最独特的增量（G-3 证据落盘、G-4 guardrail 双侧、G-5 阶段摘要外部记忆、G-2 常驻生命周期），调研深度最高（含横向对比表）。失分在核心判断偏保守——坚持"共享 JSON + 轮询保留"，低估了 Hermes 常驻服务替代 Codex 线程的彻底性。
3. **WorkBuddy（78 分）**：方向判断最准（"09 机制层裁剪、状态机层保留"的二分法清晰），对 09/07/README 的逐项判定准确。失分在完整性——无优先级落地顺序、无术语映射、补漏项粒度偏粗。

---

## 第五部分 · 无法回答的问题【待解决】

> 以下问题我在审阅中无法凭现有材料判断，如实记录，不猜测。

1. **【待解决】台账载体归属**：Hermes 台账用自带 DB 还是项目本地文件？三方大纲均未实测，deepseek 默认"仍是共享 JSON 文件"，mimo 默认"SQLite/文件"，WorkBuddy 未预判。**需 Hermes 能力边界清单实测后定。**
2. **【待解决】「0磋」语义**：Richy 工作稿 §七-4 标记语义未知，三份大纲均未给出结论。**需向原文档作者确认。**
3. **【待解决】Hermes 持久化与事件能力**：mimo F-1 指出"Hermes 持久化能力未实测"。Hermes 是否真自带状态持久化、事件源覆盖范围（飞书/webhook 是否足够替代全部巡检场景），我无法从规范文本判断。**需实测。**
4. **【待解决】单实例 vs 多实例**：三方均默认单实例，但 Hermes 未来是否可能多实例部署未确认。**需 Hermes 确认运行形态。**
5. **【待解决】deepseek 的 G-6（config_change 通知）与 mimo 的 L-5（并行派发）是否纳入 V2.5 主干**：我判断为"需商榷"的增强项，是否落地取决于 Hermes 实际复杂度，无法单凭规范文本定论。

---

*本报告为审阅意见，不修改仓库任何规范正文。*
