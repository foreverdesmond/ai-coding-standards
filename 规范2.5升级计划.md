# 规范 V2.5 升级计划（Hermes 总调度模式）

> 状态：草稿（供 Hermes 总调度审阅，执行由 Hermes 另行安排）
> 起草人：WorkBuddy（规范总审阅）
> 基线：ai-coding-standards V2.4（待审核）
> 目标版本：V2.5
> 日期：2026-08-20

---

## 一、V2.5 完整目录与文件结构

```text
ai-coding-standards/                          # 根目录
├── README.md                                  # 【修改】总纲 V2.5
├── AGENTS.md                                  # 【新增】Context L1 标准入口（closest-wins）
├── Hermes能力边界清单.md                       # 【新增】cron/事件/台账/派发/权限 边界
├── 文档规范/
│   ├── 01-任务背景与现状分析文档规范.md          # 【保留】§5 Codex→执行Agent
│   ├── 02-需求规格文档规范.md                   # 【保留】§6 Codex→执行Agent
│   ├── 03-详细设计文档规范.md                   # 【保留】§8 Codex→执行Agent
│   ├── 04-详细开发任务文档规范.md               # 【修改】§4/§5/§8/§11/§13
│   ├── 05-文档审核与变更控制规范.md              # 【修改】§2.2/§8.2/§9.2
│   ├── 06-开发上下文包生成与使用规范.md          # 【修改】§3/§4.1/§6/§8/§12 旧名BaseSHA
│   ├── 07-子Agent任务委派与提示词规范.md         # 【修改】§2.1/§3/§5/§8
│   ├── 08-开发验证、代码审查与合并门禁规范.md     # 【修改】§3.2.1 引用同步
│   ├── 09-Hermes调度与运行时台账规范.md          # 【重写】原"09-调度控制平面与运行时台账规范.md"
│   └── 提示词模板/
│       ├── README.md                           # 【修改】模板索引与使用方法
│       ├── 00-公共基座.md                       # 【重写】→ Hermes 注入协议
│       ├── 01-开发Agent.md                      # 【修改】状态发布接口
│       ├── 02-Coding-Reviewer.md                # 【修改】状态发布接口
│       ├── 03-返工Agent.md                      # 【修改】状态发布接口
│       ├── 04-测试验证Agent.md                  # 【保留】身份字段统一
│       ├── 05-系统集成Reviewer.md               # 【保留】身份字段统一
│       ├── 06-最终合并审核Agent.md               # 【保留】身份字段统一
│       ├── 07-总调度Agent.md                    # 【重写】→ Hermes 调度配置
│       ├── 08-需求审核Agent.md                  # 【保留】身份字段统一
│       ├── 09-设计审核Agent.md                  # 【保留】身份字段统一
│       ├── 10-开发任务审核Agent.md               # 【修改】检查项去共享JSON
│       ├── 11-迭代集成执行Agent.md               # 【保留】身份字段统一
│       ├── 12-主分支合并执行Agent.md             # 【保留】身份字段统一
│       ├── 13-总调度周期巡检.md                  # 【重写】→ Hermes cronjob 配置
│       ├── 14-总调度冷恢复.md                    # 【重写】→ Hermes 重启恢复规则
│       └── 15-调度协议Canary.md                  # 【重写】→ Hermes 闭环 Canary 配置
```

---

## 二、逐文件修改清单

> 格式：**文件 → 章节 → 现状 → 改成什么 → 原因**。

### 1. README.md（修改）

| 章节 | 现状 | 改成 | 原因 |
|---|---|---|---|
| 标题/版本 | V2.4 待审核 | V2.5 待审核，标注"Hermes 总调度基线" | 版本演进 |
| §2.10 文档状态驱动调度 | "调度者和周期巡检只轮询共享 JSON 的 StateRevision" | 改为"文档状态驱动调度；Hermes 台账记录状态，事件驱动 + cron 定期对账消费" | 去掉 Codex 共享 JSON 实现细节，保留精神 |
| §2.11 调度状态持久可恢复 | "共享一个 SharedRuntimeStatePath；通过 StateUpdateToolPath 短时锁原子写入" | 改为"调度状态持久于 Hermes 台账，可恢复可审计；不再有共享 JSON 文件锁" | 同上 |
| §3 规范目录表 | 09 名称为"调度控制平面与运行时台账规范" | 改为"09-Hermes调度与运行时台账规范" | 文件重命名 |
| §10 Codex 与多 Agent 协作规则 | 16 条中 8–11 条含"共享本地 JSON/统一原子工具/周期巡检/Codex 子任务" | 整节重写为"Hermes 总调度与多 Agent 协作规则"：Hermes 台账、cron+事件、单实例幂等、重启恢复、WB 写/Codex 审 | Codex 专属词汇全替换 |
| §13 V2.4 待审核决定 | DOC-DEC-014~020 为 09 共享 JSON 相关 | 改写为 V2.5 待审核决定：DOC-DEC-021（台账替代共享JSON）、022（单实例+DispatchKey 替代租约）、023（事件驱动+cron 兜底替代周期巡检）、024（枚举四档） | 迁移决议更新 |

### 2. AGENTS.md（新增）

**内容**：定义 Context L1 标准入口。
- 位置：仓库根 `AGENTS.md`；子目录可嵌套，closest-wins（离被编辑文件最近者优先）。
- 优先级：用户当前显式指令 > 就近 AGENTS.md > 远程文档。
- 内容约定：项目技术栈、构建/测试命令、编码规范、分支策略、工具权限。
- 与 06 上下文包衔接：AGENTS.md 是 Context L1 物理载体。

**原因**：V2.4 README §9 仅提"可复用现有 AGENTS"，无位置/嵌套/优先级细则；AGENTS.md 已是 60k+ 项目的事实标准。

### 3. Hermes能力边界清单.md（新增）

**内容**（P0 前提，落地前必须先厘清）：
1. cron 配置能力与精度；
2. 事件来源清单（飞书推送 / webhook / 其他）及覆盖范围；
3. 台账存储位置（自带 DB vs 项目本地文件）与 schema 能力；
4. Agent 派发 API（如何拉起 WorkBuddy / Codex / Human）；
5. 工具权限分级（只读/读写/危险）如何下发；
6. 重启恢复语义（如何读台账 + Git 重建）。

**原因**：三份大纲均假设 Hermes 能力，但未实测（mimo F-1、工作稿 F 条）。无此清单，台账 schema 与调度循环无法落地。

### 4. 文档规范/01、02、03（保留 + 微调）

| 文件 | 章节 | 改成 |
|---|---|---|
| 01 | §5 "Codex 执行要求" | 标题与正文"Codex"改为"执行 Agent（WorkBuddy/Codex）"，9 条要求本身通用不变 |
| 02 | §6 "Codex 执行要求" | 同上，9 条要求不变 |
| 03 | §8 "Codex 执行要求" | 同上，9 条要求不变 |

**原因**：这些"执行要求"是编写文档时的行为准则，与调度机制无关，仅需去掉"Codex"单点绑定。

### 5. 文档规范/04-详细开发任务文档规范.md（修改）

| 章节 | 现状 | 改成 | 原因 |
|---|---|---|---|
| §4 项目级执行配置 | `InspectionMode: SharedLocalStatePolling + Watchdog`、`SharedRuntimeStatePath`、`TaskStateWriteLockPath`、`StateUpdateToolPath`、`TaskBranchPattern: codex/<...>` | 删共享 JSON 三件套；`TaskBranchPattern` 泛化；新增 `LedgerLocation`（Hermes 台账）、`DispatchMode: EventDriven + CronFallback` | 去 Codex 线程实现 |
| §5.3.1 Git 权限 | 保留（角色只向任务分支提交） | 不变 | 通用 |
| §8 任务状态与统一状态交换面 | §8.1 共享 JSON 唯一真源、§8.2 共享本地状态最小结构、§8.3 写入与并发规则（短时锁/原子替换） | 整节改写为"Hermes 台账状态面"：台账为唯一真源；开发任务文档 `TASK-STATE-EXCHANGE` 块仍为持久快照；删短时锁/原子替换，改"Hermes 台账 API 幂等写入 + DispatchKey 去重" | 状态语义保留，实现换载体 |
| §8.4 稳定任务状态 | 保留状态枚举 | 不变 | 通用 |
| §11 任务简报模板 | `ExpectedExecutionKind: CodexThread / ApprovedEquivalent` | 改为 `WorkBuddy / Codex / Human / ApprovedEquivalent` | 枚举四档 |
| §13 审核清单 | "状态交换区缺失时进入 RecoveryOnly，指定 Codex 子任务不被替代" | 改为"Hermes 台账可用；指定执行机制不被未经授权替代" | 去 Codex 专属 |

### 6. 文档规范/05-文档审核与变更控制规范.md（修改）

| 章节 | 现状 | 改成 | 原因 |
|---|---|---|---|
| §2.2 Codex | "Codex 负责调查、提问、编辑文档" | 改为"执行 Agent（WorkBuddy/Codex）负责…"，职责通用不变 | 去单点绑定 |
| §8.2 任务状态 | "共享 JSON 为唯一真源、RecoveryOnly 恢复" | 改为"Hermes 台账为唯一真源，缺失时从文档快照 + Git 恢复" | 实现换载体 |
| §9.2 开发证据 | `BaseSHA` 兼容旧名说明 | 保留 `CodeBaseSHA`，`BaseSHA` 标记废弃 | 与 06 同步 |

### 7. 文档规范/06-开发上下文包生成与使用规范.md（修改）

| 章节 | 现状 | 改成 | 原因 |
|---|---|---|---|
| §3 Context L1 | "可复用 AGENTS.md、ADR…" | 明确指向根 `AGENTS.md` 为标准入口 + closest-wins | 补漏 L-1 |
| §4.1/§6/§8/§12 模板 | 旧名 `BaseSHA` | 统一为 `CodeBaseSHA` | 与 05/09 术语一致 |

### 8. 文档规范/07-子Agent任务委派与提示词规范.md（修改）

| 章节 | 现状 | 改成 | 原因 |
|---|---|---|---|
| §2.1 执行机制 | `ExpectedExecutionKind: CodexThread / SubAgent / Human / ApprovedEquivalent` | 改为 `WorkBuddy / Codex / Human / ApprovedEquivalent`；删"创建 Codex 子任务失败不得 spawn_agent 兜底"，改"禁止伪独立自审" | 枚举四档 + 去 Codex 专属禁令 |
| §3 提示词分层 | 保留公共基座+角色增量结构 | 不变（骨架适用） | 与 subagent frontmatter 同构 |
| §4 提示词组装 | 动态数据（Invocation/diff/Findings） | 加"动态数据由 Hermes 注入" | 适配常驻调度 |
| §5 角色独立性 | 载体分离 | 加"载体分离 ≠ 视角独立，保留独立风险模型" | 工作稿 D 条 |
| §8 总调度最低规则 | "轮询 SharedRuntimeStatePath" | 改"Hermes 台账 + 事件驱动 + cron 兜底"；依赖图由 Hermes 主动触发下游 | 适配常驻调度 |
| §8.4 Finding 三次升级 | "Coordinator 计数判断" | 保留"第2轮分类根因→第3轮停机械返工上报裁决" | 工作稿 E 条 |

### 9. 文档规范/08-开发验证、代码审查与合并门禁规范.md（修改）

| 章节 | 现状 | 改成 | 原因 |
|---|---|---|---|
| §3.2.1 Review 执行状态 | "控制面执行故障以 09 为准" | 引用改为"09-Hermes调度与运行时台账规范" | 随 09 重写同步 |

### 10. 文档规范/09（重写整篇）

**原文件**：`09-调度控制平面与运行时台账规范.md`
**新文件**：`09-Hermes调度与运行时台账规范.md`

| 新章节 | 内容（保留/改写来源） |
|---|---|
| 1. 目的与单一职责 | 规定 Hermes 台账、调度循环、派发/消费、恢复与 Canary |
| 2. 核心原则 | 保留"文档状态驱动/状态分离/持久可恢复/最大安全状态/控制面故障不污染业务"；删"明确执行机制不可替代"中的 Codex 绑定 |
| 3. 三类状态 | 保留 ThreadStatus（改名"执行载体状态"）/TaskState/EvidenceState 及禁止推断清单 |
| 4. 不可变执行基线 | 保留 CodeBaseSHA 等基线字段 |
| 5. 台账状态面 | **改写**：Hermes 台账 schema（任务/阶段/状态/证据/依赖图）；`TASK-STATE-EXCHANGE` 块仍为 Git 持久快照；删短时锁/原子替换 |
| 6. 事件与幂等 | 保留 `DispatchKey = IterationID+TaskID+Stage+TargetIdentity`、`RecordID+SignalRevision` 幂等消费、事件指纹 |
| 7. 派发与身份绑定 | **改写**：Hermes 派发 API + `ExecutionRef` 统一身份；删 clientThreadId 映射 |
| 8. 状态消费协议 | 保留消费循环语义；删"轮询 StateRevision"，改"事件驱动 + cron 定期对账兜底" |
| 9. 证据归属与冲突 | 保留（来源职责表） |
| 10. Review 执行状态与 Verdict 分离 | 保留 |
| 11. 周期巡检 | **重写**：Hermes cron 对账规则，替代独立巡检任务；保留"无待消费不遍历" |
| 12. 暂停与并发 | **改写**：保留 Paused 语义；删协调租约锁，改"单实例 + DispatchKey 幂等去重" |
| 13. 恢复协议 | **改写**：保留热/冷/灾难三级与最大安全状态；删"从共享 JSON 重建"，改"Hermes 重启读台账 + Git 重放" |
| 14. 协议 Canary | **改写**：保留 14 场景精神，新增常驻场景（多实例防重/事件丢失后 cron 兜底/Hermes 热恢复）；删"要求 Codex 子任务失败不兜底" |
| 15. 审核清单 | 同步更新 |
| 16. 完成定义 | 同步更新 |

### 11. 提示词模板/00-公共基座.md（重写）

**改成**：Hermes 注入协议。
- 协议身份：`ProtocolVersion`、`IterationID`、`TaskID`、`InvocationID`、`ParentCoordinatorRef`（Hermes 身份）。
- 执行机制：`ExpectedExecutionKind: WorkBuddy/Codex/Human/ApprovedEquivalent`。
- 台账：`LedgerLocation` + `StateRecordID`（替代 SharedRuntimeStatePath + StateUpdateToolPath）。
- 删：短时锁、原子替换、`PendingConsumption` 由工具写共享 JSON 的 10 条细则，改为"Hermes 台账接口幂等写入"。
- 保留：保护用户改动、外部操作授权、NotRun、Git 权限、结构化协议头、错误目标停止。

**原因**：00 基座耦合 Codex 字段最重，是 01/02/03 等所有角色模板的公共依赖，必须先行重写。

### 12. 提示词模板/01、02、03（修改）

| 文件 | 章节 | 改成 |
|---|---|---|
| 01 开发Agent | 输出块末尾"通过 StateUpdateToolPath 写 SharedRuntimeStatePath 发布 PendingConsumption" | 改为"通过 Hermes 台账接口写 StateRecordID 并发布待消费信号" |
| 02 Coding-Reviewer | 同上 | 同上 |
| 03 返工Agent | 同上 | 同上 |

**原因**：角色契约通用，仅状态发布接口换载体。

### 13. 提示词模板/04、05、06、08、09、11、12（保留 + 身份字段统一）

| 文件 | 改动 |
|---|---|
| 04 测试验证 / 05 系统集成 Reviewer / 06 最终合并审核 / 08 需求审核 / 09 设计审核 / 11 迭代集成 / 12 主分支合并 | 无实质改动，仅把 `CoordinatorThreadID`（如出现）统一为 `ParentCoordinatorRef`，`ProtocolVersion` 等身份字段保持 |

### 14. 提示词模板/07-总调度Agent.md（重写）

**改成**：Hermes 调度配置与指令（非提示词驱动的临时线程）。
- 固定身份：Hermes 服务身份 + 恢复点（替代 CoordinatorThreadID）。
- 调度循环：启动加载台账 → idle 等待（事件驱动）→ cron 兜底对账 → 消费待处理记录 → 依赖满足触发下游。
- 删：共享 JSON 验证、租约获取、StateRevision 轮询、RecoveryOnly 从 JSON 重建。
- 保留：派发前清单、幂等 DispatchKey、Review 执行状态与 Verdict 分离、Finding 三次升级、暂停语义。

### 15. 提示词模板/13-总调度周期巡检.md（重写）

**改成**：Hermes cronjob 对账配置。
- cron 规则（周期、停止条件、通知条件）。
- 每轮：读台账 → 处理待消费记录 → 健康检查（台账完整性、Git 祖先关系）→ 无待消费不遍历。
- 删：短时锁、原子替换、Watchdog 计数、共享 JSON 字段。
- 保留：证明"无变化"所需的最小证据清单（TaskDocumentPath/StateRevisionBefore/After/PendingRecords/TargetedThreadsRead/GitHeadsChecked）。

### 16. 提示词模板/14-总调度冷恢复.md（重写）

**改成**：Hermes 重启恢复规则（配置）。
- 触发：台账缺失/损坏/版本回退。
- 步骤：读不可变任务定义 → 扫描 Git → 对缺失证据记录定向发现 → 计算最大安全状态 → 写回台账 → 等 RecoveryApproved。
- 删：共享 JSON 重建、短时锁写回。
- 保留：最大安全状态规则表（仅 commit=EvidenceIncomplete、Approved 匹配 Head=TaskAccepted 等）。

### 17. 提示词模板/15-调度协议Canary.md（重写）

**改成**：Hermes 闭环 Canary 配置（Hermes→WB→Codex→验收）。
- 保留 14 场景精神：两段式创建、早完成消费、缺字段补发、幂等去重、导入证据核验、ToolRuntime 不污染、新 Head 失效、Paused 无副作用、热恢复、删台账不丢状态、多 worktree 一致、并发防重。
- 新增常驻场景：多实例防重、事件加速丢失后 cron 兜底、Hermes 重启热恢复。
- 删：隔离共享 JSON、隔离锁路径、Codex 创建机制兜底禁令。

---

## 三、落地优先级

| 优先级 | 动作 | 依赖 |
|---|---|---|
| P0 | 写 `Hermes能力边界清单.md`（实测能力） | 无 |
| P0 | 重写 `00-公共基座.md` 为 Hermes 注入协议 | P0 清单 |
| P1 | 重写 `09` 为 Hermes 调度与运行时台账规范 | P0 清单 |
| P1 | 新增 `AGENTS.md` + 修改 `06`（Context L1 入口） | 无 |
| P2 | 修改 `07`、`04`、`05`（枚举/状态面/去共享JSON） | P1 的 09 |
| P2 | 重写 `07/13/14/15` 提示词模板 | P1 的 09 |
| P3 | 修改 `README`、`01/02/03` 措辞 | P2 |
| P3 | 微调 `08` 引用 + 其余模板身份字段 | P2 |

---

*本计划为规范升级蓝图，具体实施由 Hermes 总调度另行派发任务，不在本文件内 commit。*
